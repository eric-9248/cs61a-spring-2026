1.7 递归函数
定义：如果函数体直接或间接调用了自身，则该函数为递归函数。Python 中无需特殊语法即可实现。
核心思想：将问题分解为更简单的子问题。例如计算数字各位之和时，利用 % 和 // 运算符将数字拆分为“最后一位”和“剩余部分”，通过递归处理剩余部分并加上最后一位来求解。
执行模型：递归函数的正确性可以通过环境模型解释。每次递归调用都会创建新的局部帧，参数规模逐渐减小，直到触发基准情况，随后逐层返回结果。简单的函数体可通过递归演化出复杂的计算过程。
1.7.1 递归函数剖析
基本结构：通常包含“基准情况”（最简单输入的直接处理）和“递归调用”（使问题变简化的自我调用）。
与迭代的区别：迭代通过维护局部状态（如累加器变量）逐步构建结果；递归则通过函数抽象，依赖返回值层层传递状态，无需显式维护额外变量。
信仰之跃：验证递归正确性时，不必追踪底层执行细节，只需假设对更简单输入的递归调用是正确的（即归纳法思维），从而简化理解与验证过程。
优势：利用绑定规则避免了迭代中手动维护状态的繁琐，通常更容易编写正确。
1.7.2 互递归
定义：两个或多个函数互相调用形成的递归过程。
特点：互递归并不比简单递归更强大，可以转化为单一递归函数，但它提供了一种在复杂程序中保持抽象屏障的有效机制。
应用示例：如判断奇偶数、模拟双人回合制游戏策略等，通过将不同逻辑封装在不同函数中来体现交互性。
1.7.3 递归函数中的打印
可视化：通过在递归调用前后添加 print 语句，可以直观展示递归演化的计算过程（如级联打印数字前缀）。
代码优化：若基准情况和递归分支中有重复操作（如打印当前值），可将该操作提取到条件判断之前以简化代码。
抽象屏障：在互递归示例（如石子游戏）中，将不同玩家的策略封装为独立函数，修改某一策略不会影响另一策略的实现。
1.7.4 树形递归
定义：函数在一次调用中直接产生多次递归调用。
特征：调用结构呈树状分叉，随着递归深度增加，子问题数量指数级增长。
适用场景：虽然效率可能较低（如斐波那契数列的朴素递归），但对于某些问题（如分割数），树形递归比迭代方案更直观、更易实现。
